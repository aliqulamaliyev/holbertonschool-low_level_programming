#include <stdio.h>
/**
 * main - prints the given sentence starting by a new line
 *
 * Return: Always (succesful)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
