#include <stdio.h>
/**
 * main - prints with proper grammar ,but the outcome is a piece of art starting by a new line  
 *
 * Return: Always (succesful)
 *
 */
	int main(void)
	{
   		 printf("with proper grammar, but the outcome is a piece of art,\n");
  		 return (0);
	}
